services:
  db:
    image: postgres:16-alpine
    container_name: bubble-tea-db
    environment:
      POSTGRES_DB: bubble_tea
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: bubble-tea-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: bubble-tea-api
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/bubble_tea
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:-your-super-secret-jwt-key}
      JWT_EXPIRES_IN: "7d"
      NODE_ENV: production
      PORT: 3000
      CORS_ORIGIN: ${CORS_ORIGIN:-https://admin.aicube.online,https://pos.aicube.online,https://staff.aicube.online,https://api.aicube.online}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  admin:
    build:
      context: ./client-admin
      dockerfile: Dockerfile
    container_name: bubble-tea-admin
    ports:
      - "5173:80"
    restart: unless-stopped

  pos:
    build:
      context: ./client-pos
      dockerfile: Dockerfile
    container_name: bubble-tea-pos
    ports:
      - "6063:80"
    restart: unless-stopped

  staff:
    build:
      context: ./client-staff
      dockerfile: Dockerfile
    container_name: bubble-tea-staff
    ports:
      - "5175:80"
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:

networks:
  default:
    name: bubble-tea-network
